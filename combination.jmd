This file will is a replacement for Bliss and contains combination results. All this is for replicate 1. We could average the three and report the results or plot the three replicates separately.


```julia; echo=false
using DrugResponseModel
using Plots

concs, populations, g1s, g2s = load(189, 1);
g0 = g1s[1,1,1]+g2s[1,1,1];
```

```julia; echo=false
pp = [61.7888, 0.984496, 0.00365409, 1.43353, 0.020916, 0.0499489, 
        22.8587, 3.56592, 2.316, 0.00665677, 0.402978, 0.384546, 
        17.2862, 2.39072, 1.34065, 0.999105, 0.0391467, 0.237266, 
        3.16643, 9.96997, 0.397842, 0.0697343, 0.508761, 0.340036, 
        112.903, 0.902078, 0.0722487, 1.91693, 0.0675425, 0.102127, 
        1.89566, 0.729827, 0.521269, 41.0931, 24.153, 36.7235, 13.4195];
```

```julia; echo=false
effs = getODEparamsAll(pp, concs)
lapat = effs[:,:,1];
dox = effs[:,:,2];
gemc = effs[:,:,3];
pac = effs[:,:,4];
pal = effs[:,:,5];
cellnum=blissCellNum(g1s, g2s)
LD = cellnum[:,:,1]; # lap and dox
LG = cellnum[:,:,2]; # lap anf gem
LP = cellnum[:,:,3]; # lap and pac
LPA = cellnum[:,:,4]; # lap and palb
DG = cellnum[:,:,5]; # dox and gem
DP = cellnum[:,:,6]; # dox and pac
DPA = cellnum[:,:,7]; # dox and palb
GP = cellnum[:,:,8]; # gem and pac
GPA = cellnum[:,:,9]; # gem and palb
PPA = cellnum[:,:,10]; # pac and palb
```

The heatmap shows the normalized absolute difference between the results from our model; which is calculating cell number after applying Bliss independence on the model parameters (or simply drug effects), versus applying the Bliss formulation directly to the cell number which is standard way of drug combination calculation. The x and y axis show the log concentration of the two drugs, and the brighter the color gets, the more difference between the model and the conventional combination calculations are.

## Lapatinib and doxorubicin

```julia; echo=false
DrugResponseModel.heatmap_combination(lapat, dox, LD', 1, 2, "lapatinib", "doxorubicin", effs, concs, g0)
```

## Lapatinib and gemcitabine

```julia; echo=false
DrugResponseModel.heatmap_combination(lapat, gemc, LG', 1, 3, "lapatinib", "gemcitabine", effs, concs, g0)
```

## Lapatinib and paclitaxel

```julia; echo=false
DrugResponseModel.heatmap_combination(lapat, pac, LP', 1, 4, "lapatinib", "paclitaxel", effs, concs, g0)
```

## Lapatinib and palbociclib

```julia; echo=false
DrugResponseModel.heatmap_combination(lapat, pal, LPA', 1, 5, "lapatinib", "palbociclib", effs, concs, g0)
```

## Doxorubicin and gemcitabine

```julia; echo=false
DrugResponseModel.heatmap_combination(dox, gemc, DG', 2, 3, "doxorubicin", "gemcitabine", effs, concs, g0)
```

## Doxorubicin and paclitaxel

```julia; echo=false
DrugResponseModel.heatmap_combination(dox, pac, DP', 2, 4, "doxorubicin", "paclitaxel", effs, concs, g0)
```

## Doxorubicin and palbociclib

```julia; echo=false
DrugResponseModel.heatmap_combination(dox, pac, DPA', 2, 5, "doxorubicin", "palbociclib", effs, concs, g0)
```

## Gemcitabine and paclitaxel

```julia; echo=false
DrugResponseModel.heatmap_combination(gemc, pac, GP', 3, 4, "gemcitabine", "paclitaxel", effs, concs, g0)
```

## Gemcitabine and palbociclib

```julia; echo=false
DrugResponseModel.heatmap_combination(gemc, pal, GPA', 3, 5, "gemcitabine", "palbociclib", effs, concs, g0)
```

## Paclitaxel and palbociclib

```julia; echo=false
DrugResponseModel.heatmap_combination(pac, pal, PPA', 4, 5, "paclitaxel", "palbociclib", effs, concs, g0)
```